package tud.tangram.svgplot.legend;

/**
 * Types of {@link LegendItem LegendItems}. The order is importantant, as the
 * elements will appear in the legend in this order.
 */
public enum LegendItemType {
	TITLE, POINT, LINE, AREA, TEXT
}
